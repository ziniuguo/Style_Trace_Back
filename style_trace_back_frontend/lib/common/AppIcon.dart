import 'package:flutter/material.dart';

class AppIcon {
  static const bottomNavSu = Icon(Icons.home);
  static const bottomNavStreet = Icon(Icons.people);
  static const bottomNavMe = Icon(Icons.account_circle_outlined);
  static const topBarBack = Icon(Icons.chevron_left, size: 40);
  static const searchIcon = Icon(Icons.search);
  static const returnTopIcon = Icon(Icons.navigation);
}
